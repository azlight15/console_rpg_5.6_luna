# 开发笔记

***

## 版本记录

### Luna v1.0.0

v1.0 的重点不是继续堆大型系统，而是让 v0.5 已经存在的战斗、装备、升级和存档形成一个完整的小型 RPG 循环，并把代码整理到适合长期回看的程度。

主要变化：

- 新增金币系统，怪物根据自身等级给予金币奖励。
- 新增 `Shop`，玩家可以使用金币购买治疗资源、技能点和基础装备。
- 新增技能点资源，技能根据 `SkillPointCost` 消耗不同数量的技能点。
- 升级后技能点补满，战斗胜利后恢复 1 点技能点。
- 战斗界面、状态界面和启动确认界面增加金币与技能点信息。
- 存档新增金币和技能点字段；旧档缺少字段时使用默认值继续读取。
- 怪物数据新增金币奖励，并随怪物等级进行成长。
- 对 `Program`、`Player`、`Battle`、`SkillSystem`、`DamageCalculator`、`Monster`、`MonsterFactory`、`Equipment`、`EquipmentManager`、`LevelUp`、`ShowStatus`、`SaveData` 等文件补充解释性注释。
- README 增加代码阅读路线，让一年后重新打开项目时可以从 `Program → Player → Battle` 顺着理解。

### v1.0 的代码注释原则

注释的目标不是把每一行 C# 翻译成中文，而是回答“未来的自己最容易忘记什么”：

1. **这个类负责什么？** —— 写在类的 XML summary。
2. **这个方法为什么存在？** —— 解释它和其他系统的职责边界。
3. **为什么这里这么写？** —— 解释 while、工厂、封装、兼容旧档等关键设计。
4. **这个数字是什么？** —— 对升级曲线、技能消耗、掉落率等规则写明含义。
5. **哪里不能直接改？** —— 说明例如 Player 私有 setter 和最终属性计算的原因。

这样注释是在帮助理解设计，而不是重复代码本身。

***

### Luna v0.5.0

本版本把 v0.4.0 的装备与战斗基础扩展成完整的核心 RPG 循环。

主要变化：

- 怪物从 6 种扩展到 10 种，加入蝙蝠、兽人、冰霜巨兽、毒蛇。
- 怪物等级根据玩家等级生成，默认在玩家等级上下 2 级范围内随机浮动。
- 不同怪物继续拥有不同的基础定位和属性。
- 怪物最终 HP、攻击和经验会根据实际等级进行缩放。
- 保留精英怪机制，并让精英怪进一步获得等级与属性强化。
- `Equipment` 新增普通、稀有、史诗稀有度，以及暴击和闪避加成。
- 新增 `EquipmentFactory`，负责随机装备模板、稀有度和掉落装备生成。
- `Player` 新增装备库存。
- `EquipmentManager` 从固定装备目录升级为背包查看与装备选择。
- 战斗胜利后有概率获得装备，精英怪的掉落概率更高。
- 玩家最终暴击率和最终闪避率会计入装备属性。
- 怪物攻击现在会检查玩家闪避。
- 经验需求改为随等级逐步增加。
- `SaveData` 持久化完整装备库存。
- v0.4 旧档案没有 `Inventory` 字段时，会把当前武器和防具补回库存。

***

### Luna v0.4.0

本版本正式把装备和技能从数据模型接入游戏流程。

主要变化：

- 新增 `EquipmentManager`，提供装备界面。
- 新角色默认装备木剑和旅行皮甲。
- `Player` 通过 `FinalAttack`、`FinalMaxHp`、`FinalCriticalRate` 和 `FinalEvasionRate` 计算最终属性。
- 装备不会直接修改基础属性，避免更换装备时产生属性残留。
- 新角色默认学习重击和火球。
- `Battle` 新增技能行动。
- `SkillSystem` 负责技能执行，`DamageCalculator` 统一处理伤害。
- 普通攻击和技能攻击支持怪物闪避和暴击。
- `ShowStatus` 增加装备、技能和最终属性显示。
- `SaveData` 增加装备与技能持久化。
- 开始补充代码注释，明确系统职责。

***

### Luna v0.3.0

本版本从基础架构整理进入战斗玩法扩展阶段。

主要变化：

- 怪物系统增加战斗定位。
- 新增更多怪物类型：史莱姆、哥布林、骷髅兵、野狼、巨魔、黑暗法师。
- 怪物模型增加闪避属性。
- 战斗系统加入暴击和闪避机制。

***

### Luna v0.2.0

本版本继续进行玩家状态架构重构，并开始处理第一轮评测指出的资源管理与状态封装问题。

主要变化：

- `PlayerStatistics` 全局静态状态正式由 `Player` 实例取代。
- `Player` 开始负责维护自身 HP。
- 治疗资源统一归玩家持有。
- 战斗内与场外治疗共享治疗资源。
- 升级时恢复满 HP，并补充治疗资源。
- 存档新增治疗资源字段。

***

## 关于 Console_RPG

这是一个个人开发的控制台回合制 RPG。

Luna 版本是在原项目基础上的独立重构版本。当前重点是降低全局状态耦合，同时逐步扩展战斗、装备、经济和成长系统。

***

## 当前系统说明

### Program.cs

负责启动、角色创建/读取和主菜单分发。

它的核心原则是：**Program 决定“调用谁”，不要自己实现所有游戏规则。**

### Player.cs

`Player` 保存玩家运行时状态，并提供修改状态的行为方法。

基础属性、最终属性、装备、背包、技能、治疗资源、金币和技能点都集中在这里。

### Equipment.cs / EquipmentFactory.cs

`Equipment` 保存装备本身的数据。

`EquipmentFactory` 负责从模板随机生成掉落装备和稀有度。

### EquipmentManager.cs

负责背包和装备相关 UI。真正的装备状态仍然保存在 `Player` 中。

### Battle.cs

负责回合制战斗流程，包括行动选择、怪物反击、胜负、EXP、金币和装备掉落。

### SkillSystem.cs / DamageCalculator.cs

`SkillSystem` 负责技能规则和资源消耗，`DamageCalculator` 负责伤害数值。

### Monster.cs / MonsterFactory.cs

`MonsterStatistics` 是战斗中一只怪物的当前状态。

`MonsterFactory` 决定怪物种类、等级、属性和奖励。

### LevelUp.cs

负责经验结算和升级流程；具体属性成长交给 `Player.ApplyLevelUp()`。

### Shop.cs

负责金币消费 UI。商店本身不保存金币，金币仍由 `Player` 管理。

### SaveData.cs

负责 JSON DTO 与本地档案管理。

`SaveData.FromPlayer()` 把运行时状态转换成存档数据；`ApplyTo()` 再交给 `Player.RestoreFromSave()` 恢复。

***

## 下一阶段

v1.0 完成后，下一阶段可以根据实际游玩结果选择方向，而不是为了版本号强行增加系统。

候选方向：

- 自动化测试项目。
- 真正的技能冷却和状态效果。
- Boss / 精英战斗行为。
- 装备出售、强化和更完整的词缀系统。
- 任务与地图。
- 更进一步的数值平衡。
- UI 与输入系统继续整理。
