# 开发笔记

***

## 关于 Console_RPG

这是一个个人开发的控制台回合制 RPG。

这一版在保留原项目核心玩法的基础上，继续整理角色状态、输入处理、战斗、升级、怪物生成和存档结构，并开始强化资源管理与角色封装。

***

## 这个项目有哪些系统？

### 1. `Program.cs`

负责程序入口、创建当前 `Player` 实例、开始菜单和主菜单。

当前的玩家状态由 `Program` 持有，并通过参数传给其他系统，而不是使用全局静态的 `PlayerStatistics`。

### 2. `Battle.cs`

负责回合制战斗。

主要流程：

1. 从主菜单进入战斗。
2. `MonsterFactory.Create(player)` 根据当前玩家等级生成敌人。
3. 玩家可以攻击、治疗或撤退。
4. 治疗资源由 `Player` 统一管理，战斗内与场外治疗共用同一资源。
5. 击败敌人后获得经验，并由 `UpLevel` 处理升级。
6. 战斗胜利后可以选择继续寻找下一个敌人。

无效行动不会消耗回合，也不会直接结束战斗。

### 3. `LevelUp.cs`

负责经验值和升级。

`GainExp` 会处理一次经验奖励可能带来的多次升级，并保留升级后溢出的经验。

升级目前会提高最大 HP 和攻击力、恢复满 HP，并额外获得 1 次治疗资源。

### 4. `Heal.cs`

负责主菜单中的场外治疗。

治疗会消耗 `Player` 持有的治疗资源，并与战斗中的治疗共享资源池，因此不能通过反复离开战斗来无限恢复 HP。

### 5. `ShowStatus.cs`

负责显示玩家名称、等级、经验、HP、攻击力、单次治疗量和剩余治疗资源。

### 6. `Player.cs` 和 `Monster.cs`

`Player` 表示玩家角色及其运行时状态。

`MonsterStatistics` 表示单个敌人的基础战斗数据。

玩家 HP 现在由 `Player` 自己封装，通过 `TakeDamage`、`Heal`、`UseTreatment` 等行为修改，而不是允许其他系统直接赋值。这样可以集中保证 HP 不会低于 0 或超过最大值。

角色状态已经从原来的全局静态 `PlayerStatistics` 改为 `Player` 实例，后续扩展装备、技能等系统时可以继续沿着实例化对象的方向演进。

### 7. `MonsterFactory.cs`

负责创建随机敌人。

当前包含史莱姆、哥布林和骷髅兵三种基础怪物，并有 10% 概率生成精英变体。

怪物属性会根据玩家等级进行缩放。

### 8. `SaveData.cs`

负责 JSON 存档。

`SaveData` 是独立的存档数据模型：

- `FromPlayer` 将玩家实例转换成存档快照。
- `ApplyTo` 将经过验证的存档数据应用回玩家实例。
- `SaveManager` 负责文件读写和异常处理。
- 治疗资源数量也会随存档保存和恢复。

***

## 当前仍可以改进什么？

- 增加更多具有不同战斗定位的怪物，而不只是增加名称和数值。
- 加入命中、闪避、暴击等战斗变化。
- 加入装备、技能和更多道具系统。
- 进一步丰富战斗中的策略选择。
- 重新设计经验和数值成长曲线。
- 为 `Player` 和核心战斗逻辑补充自动化测试。

这一步先解决治疗资源无限的问题，并进一步收紧 `Player` 的状态封装，为下一阶段的战斗机制扩展打基础。
