# 开发笔记

***

## 版本记录

### Luna v0.2.0

本版本继续进行玩家状态架构重构，并开始处理第一轮评测中指出的资源管理与状态封装问题。

主要变化：

- `PlayerStatistics` 全局静态状态正式由 `Player` 实例取代。
- `Player` 开始负责维护自身 HP，外部系统通过行为方法修改状态。
- 治疗资源统一归玩家持有。
- 战斗内与场外治疗共享治疗资源，避免无限场外回血。
- 升级时恢复满 HP，并补充治疗资源。
- 存档新增治疗资源字段。
- 战斗、治疗、升级、状态显示和存档注释同步更新。

这一版的目标不是大量增加玩法，而是继续把基础架构整理干净，为后续战斗机制扩展做准备。

***

## 关于 Console_RPG

这是一个个人开发的控制台回合制 RPG。

Luna 版本是在原项目基础上的独立重构版本。当前重点是逐步降低全局状态耦合，同时让角色对象自己负责维护角色状态。

***

## 这个项目有哪些系统？

### 1. `Program.cs`

负责程序入口、创建当前 `Player` 实例、开始菜单和主菜单。

当前的玩家状态由 `Program` 持有，并通过参数传给其他系统，而不是使用全局静态的 `PlayerStatistics`。

### 2. `Player.cs`

`Player` 表示玩家角色及其运行时状态。

当前由 `Player` 自己维护 HP 与治疗资源。其他系统不应该直接修改这些状态，而应通过 `TakeDamage`、`Heal`、`UseTreatment`、`RestoreFullHealth` 等行为进行操作。

这样做的目的，是让 HP 的边界检查和资源消耗集中在角色对象内部，避免不同系统各自实现一套规则。

### 3. `Battle.cs`

负责回合制战斗。

主要流程：

1. 从主菜单进入战斗。
2. `MonsterFactory.Create(player)` 根据当前玩家等级生成敌人。
3. 玩家可以攻击、治疗或撤退。
4. 战斗内治疗会消耗玩家自身的治疗资源。
5. 击败敌人后获得经验，并由 `UpLevel` 处理升级。
6. 战斗胜利后可以选择继续寻找下一个敌人。

无效行动不会消耗回合，也不会直接结束战斗。

目前战斗仍然以基础攻击、治疗和撤退为主，随机战斗机制属于后续计划。

### 4. `LevelUp.cs`

负责经验值和升级。

`GainExp` 会处理一次经验奖励可能带来的多次升级，并保留升级后溢出的经验。

每次升级目前会：

- 最大 HP +20
- 攻击力 +5
- HP 完全恢复
- 治疗资源增加

### 5. `Heal.cs`

负责主菜单中的场外治疗。

场外治疗同样消耗玩家持有的治疗资源，因此不能通过反复离开战斗来无限恢复 HP。

### 6. `ShowStatus.cs`

负责显示玩家名称、等级、经验、HP、攻击力、单次治疗量和剩余治疗资源。

### 7. `Monster.cs` 和 `MonsterFactory.cs`

`MonsterStatistics` 表示单个敌人的基础战斗数据。

`MonsterFactory` 负责根据玩家等级创建随机敌人。

当前包含史莱姆、哥布林和骷髅兵三种基础怪物，并有 10% 概率生成精英变体。

下一阶段计划增加更多具有明确战斗定位的敌人，而不只是单纯提高数值。

### 8. `SaveData.cs`

负责 JSON 存档。

`SaveData` 是独立的存档数据模型：

- `FromPlayer` 将玩家实例转换成存档快照。
- `ApplyTo` 将经过验证的存档数据应用回玩家实例。
- `SaveManager` 负责文件读写和异常处理。
- 玩家治疗资源会随存档保存和恢复。

***

## 当前版本的设计重点

### 玩家状态封装

从原项目的全局静态 `PlayerStatistics` 转为 `Player` 实例后，下一步是避免其他系统直接修改玩家内部状态。

`Player` 不只是保存数据，也开始承担维护自身状态的职责。这为以后加入装备、技能、属性效果等系统提供了更明确的扩展边界。

### 治疗资源管理

此前场外治疗可以无限使用，使战斗内的治疗次数限制很容易被绕过。

v0.2.0 将治疗资源统一放入 `Player`，战斗内和场外共用同一个资源池，从规则上堵住这一漏洞。

***

## 当前仍可以改进什么？

- 增加更多具有不同战斗定位和机制的怪物。
- 加入命中、闪避、暴击等战斗变化。
- 丰富攻击、治疗之外的策略选择。
- 加入装备、技能和更多道具系统。
- 进一步设计经验和数值成长曲线。
- 为 `Player` 和核心战斗逻辑补充自动化测试。

下一阶段将从“整理地基”逐渐转向“增加玩法”，重点放在怪物机制和战斗策略上。
